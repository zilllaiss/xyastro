---
const { heading } = Astro.props 
---

<li>
    <a href={'#' + heading.slug }>{heading.text}</a>
    {heading.subheadings.length > 0 && (
        <ul>
            {heading.subheadings.map((sh: string) => (
                <Astro.self heading={sh} />
            ))}    
        </ul>
    )}
</li>